# FastAPI Server Configuration
PORT=8000
HOST="0.0.0.0"
RELOAD_DEV="false" # Set to "true" for development uvicorn reload

# API Access Key for securing sensitive endpoints
API_ACCESS_KEY="your_strong_secret_api_key_here"

# Database file name (will be placed inside CURRENT_DOWNLOAD_LOCATION)
DATABASE_FILENAME="app_data.db"

# Python logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"